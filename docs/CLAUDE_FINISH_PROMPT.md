# Thursday Post — instructions for Claude

Finish the existing Thursday Post product. Work in the private repository https://github.com/jwcoleman87-collab/thursday-post (local: `C:\Users\James Coleman\Documents\ChatGPT\Newspaper`). Inspect the latest branch and preserve other contributors' work. Live site: https://the-racing-desk.vercel.app; owner newsroom `/newsroom`, editing `/editorial/[id]`, operations `/operations`, reader paper `/news`.

Read `docs/DESIGN_PRODUCT_BRIEF.md`, `docs/CLAUDE_UI_HANDOFF.md`, `docs/MORNING_START.md` and `docs/LAUNCH_CHECKLIST.md`. Check current production readiness; historical notes about AI being paused may be superseded. This is an existing Next.js 16/React 19/TypeScript application with persistent Neon storage and 113 passing tests, not a new build.

Brand: **Thursday Post**, an Australian thoroughbred newspaper. Thursday is a horse standing at his timber post; the current mark is a draft. Use my Claude broadsheet template if available. Preserve a genuine newspaper layout, strong masthead, restrained typography, columns and readable mobile pages. Keep the subscription landing page and newspaper distinct. Reader correspondence: **workbenchadmin@gmail.com**. Keep existing GitHub/Vercel identities.

Current verified position (10 September): card verified; project `the-racing-desk` has a US$5 monthly Gateway budget; OIDC preflight succeeds with `openai/gpt-4.1-mini`. The first hosted full run FAILED: four model responses were rejected with `AI research supplied a quote absent from its source excerpt`, then immediate retries/fan-out hit HTTP 429. No researcher completed and no PE model proposal was produced in that run. Five real source records are archived; nothing was published. AI is paused again while daily source collection remains enabled. Do not describe the full live workflow as working. Existing tests use controlled model responses.

Complete these in order:

0. Fix and prove the live AI pipeline first. Inspect `src/lib/providers.ts`, `engine.ts`, `service.ts` and the private owner run audit. Give models reliable references to bounded source passages and resolve quotations server-side if necessary; do NOT weaken exact evidence checks or promote unsupported statements. Add bounded request pacing/backoff for 429 responses, respecting provider Retry-After and the whole-run deadline; prevent retry storms. Add meaningful regressions for observed live failures. Then unpause the approved production AI configuration, deploy and pass `node scripts/verify-live.mjs --run-live`: require completed research and PE calls, recorded actual usage, verified evidence and a private PE proposal, no fresh operational failures and no automatic publication. Reuse the existing OIDC identity; no new key is required. Inspect every remaining blocked story and distinguish infrastructure failure from a genuine editorial gap. Do not burn credits repeatedly on unchanged failures.

1. Make the owner workflow obvious: collect → research → evidence → PE proposal → my review → publish → assemble edition → release → SEND. Clearly distinguish Collect sources, Demo mode and Live sources. Show progress, blockers, retries, source freshness and model cost honestly.
2. Improve the research/editorial workbench: make original sources, exact supporting passages, disputed claims, missing evidence and the PE proposal easy to inspect beside editable copy. Make approve, reject and send-back actions unmistakable. Preserve every claim link, revision, review hash and explicit human attestation.
3. Address the missing commissioning workflow: I need to enter a research topic/question, jurisdiction and source URLs. First inspect what exists. If backend changes are required, implement a focused, tested owner-only extension that feeds actual permitted retrieval into the existing research pipeline. Do not add a fake chat box or claim unrestricted web/social access. Keep bounded requests and the saved US$5 monthly AI project limit; do not change models, budgets or services without approval.
4. Complete desktop/mobile newspaper presentation, horse-at-post branding, empty states, member locks and print/edition usability. Identify any missing template/artwork explicitly.
5. Work through remaining launch items and complete everything possible without new account access. Domain, Resend sending/receiving, subscriber email verification/delivery and real provider tests remain external setup work. **Stripe is on hold; sales stay closed until I set the price and explicitly reopen that work.** Never fabricate subscribers, stories, testimonials or successful signups.

Preserve server-side authentication/paywalls, immutable evidence, research/PE separation, private demo data, corrections, right-of-reply and approval gates. AI proposals must never approve themselves. Article publication, edition release and subscriber SEND require my separate decisions. Do not publish or email while testing. Never expose or commit `.env*`, `.vercel/`, `data/OWNER-ACCESS.txt`, tokens or private backups.

Verify using `npm test`, `npm run typecheck`, `npm run build`, then inspect the actual desktop/mobile owner and reader flows. Use controlled fixtures for destructive/provider scenarios; report live-provider evidence separately. Deliver tested code, a concise completed/blocked list, and a plain-English owner guide with exact buttons. Keep changes focused and token-efficient; avoid rebuilding working systems or repeatedly asking about decisions already recorded.
